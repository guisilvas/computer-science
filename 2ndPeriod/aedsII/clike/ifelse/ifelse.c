#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int x=4;

    if(x%2==0)
    {
        printf("%d is a even number",x);
    }
    else
    {
        printf("%d is a odd number",x);
    }

    return 0;
}